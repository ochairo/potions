name: flac
version:
  source: "url:https://downloads.xiph.org/releases/flac/"
  exclude_patterns: "(alpha|beta|rc|dev|preview|pre|snapshot|nightly|test|unstable|canary|next|edge|weekly|daily)"
  extract_pattern: "flac-[0-9]+\\.[0-9]+\\.[0-9]+"
  cleanup: "flac-:.tar.xz"

build_type: gnu_autotools
description: "Free Lossless Audio Codec"

download:
  download_url: "https://downloads.xiph.org/releases/flac/flac-{version}.tar.xz"
  platforms:
    darwin-x86_64:
    darwin-arm64:
    linux-amd64:
    linux-arm64:

security:
  verify_signature: false
  scan_vulnerabilities: true

configure:
  flags: ""

build:
  timeout_minutes: 20

dependencies:
  - libogg
