name: utf8proc
version:
  source: "github-release:JuliaStrings/utf8proc"
  exclude_patterns: "(alpha|beta|rc|dev|preview|pre|snapshot|nightly|test|unstable|canary|next|edge|weekly|daily)"
  extract_pattern: "v[0-9]+\\.[0-9]+\\.[0-9]+"
  cleanup: "v:"

build_type: make
description: "Clean C library for processing UTF-8 Unicode data"

download:
  download_url: "https://github.com/JuliaStrings/utf8proc/archive/v{version}.tar.gz"
  platforms:
    darwin-x86_64:
    darwin-arm64:
    linux-amd64:
    linux-arm64:

security:
  verify_signature: false
  scan_vulnerabilities: true

build:
  timeout_minutes: 10

dependencies: []
