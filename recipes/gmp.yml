name: gmp
version:
  source: "url:https://ftp.gnu.org/gnu/gmp/"
  extract_pattern: 'gmp-([0-9]+\.[0-9]+\.[0-9]+)\.tar\.xz'
  exclude_patterns: "(alpha|beta|rc|dev|preview|pre|snapshot|nightly|test|unstable|canary|next|edge|weekly|daily)"
build_type: gnu_autotools
description: "GNU multiple precision arithmetic library"

download:
  official_binary: false
  download_url: "https://ftp.gnu.org/gnu/gmp/gmp-{version}.tar.xz"
  platforms:
    darwin-x86_64:
    darwin-arm64:
    linux-amd64:
    linux-arm64:

security:
  verify_signature: false
  scan_vulnerabilities: true

configure:
  script: |
    # Extract .tar.xz if not already done
    if ls *.tar.xz >/dev/null 2>&1; then
      tar xJf *.tar.xz --strip-components=1
    fi

    ./configure --prefix="$PREFIX" \
      --disable-dependency-tracking \
      --enable-cxx

build:
  timeout_minutes: 15
  out_of_tree: false

dependencies: []
