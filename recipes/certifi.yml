name: certifi
version:
  source: "url:https://pypi.org/simple/certifi/"
  exclude_patterns: "(alpha|beta|rc|dev|preview|pre|snapshot|nightly|test|unstable|canary|next|edge|weekly|daily)"
  extract_pattern: "[0-9]+\\.[0-9]+\\.[0-9]+"
  cleanup: ""

build_type: custom
description: "Python package for providing Mozilla's CA Bundle"

download:
  download_url: "https://files.pythonhosted.org/packages/py3/c/certifi/certifi-{version}-py3-none-any.whl"
  platforms:
    darwin-x86_64:
    darwin-arm64:
    linux-amd64:
    linux-arm64:

security:
  verify_signature: false
  scan_vulnerabilities: true

configure:
  script: ""

build:
  timeout_minutes: 5
  custom_build: |
    mkdir -p "$PREFIX/lib/python3.12/site-packages"
  custom_install: |
    unzip -q certifi-{version}-py3-none-any.whl -d "$PREFIX/lib/python3.12/site-packages"
    mkdir -p "$PREFIX/bin"
    if [ -f "$PREFIX/lib/python3.12/site-packages/certifi/__main__.py" ]; then
      cat > "$PREFIX/bin/certifi" <<'EOF'
    #!/bin/bash
    python3 -m certifi "$@"
    EOF
      chmod +x "$PREFIX/bin/certifi"
    fi

dependencies:
  - python
