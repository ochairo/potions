name: "_Scheduled Release - Build Binaries macOS Intel"

on:
  workflow_call:
    inputs:
      batches:
        description: "JSON array of batches to build"
        required: true
        type: string

permissions:
  contents: read

jobs:
  build-binaries-macos-x86_64:
    name: Build Binaries (macOS Intel)
    runs-on: macos-15-intel
    timeout-minutes: 360
    env:
      GO_VERSION: "1.24"
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        batch: ${{ fromJson(inputs.batches) }}

    steps:
      - name: Checkout code
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v5

      - name: Install system dependencies
        run: |
          # Retry logic for brew in case of transient failures
          for attempt in {1..3}; do
            if brew install gpgme libgpg-error autoconf automake 2>&1 | grep -v "already installed"; then
              echo "✅ Dependencies installed"
              break
            fi
            if [ $attempt -lt 3 ]; then
              echo "⚠️  Attempt $attempt failed, retrying in 10s..."
              brew update || true
              sleep 10
            else
              echo "❌ Failed to install dependencies after 3 attempts"
              exit 1
            fi
          done

      - name: Setup Go
        uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00 # v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Check available disk space
        run: |
          echo "📊 Available disk space:"
          df -h
          echo ""
          echo "📊 Free memory:"
          vm_stat | perl -ne '/page size of (\d+)/ and $size=$1; /Pages\s+([^:]+)[^\d]+(\d+)/ and printf("%16s: %8.2f MB\n", "$1", $2 * $size / 1048576);'

      - name: Build potions CLI
        run: |
          go build -v -o bin/potions ./cmd/potions
          chmod +x bin/potions
          echo "📦 potions CLI built: $(./bin/potions --version 2>&1 || echo 'version unknown')"

      - name: Filter packages for this platform (macOS x86_64)
        id: filter-packages
        run: |
          PLATFORM="darwin-x86_64"
          ALL_PACKAGES='${{ toJson(matrix.batch.packages) }}'

          # Filter packages that support this platform (both darwin-x86_64 and macos-x86_64 formats)
          FILTERED_PACKAGES=$(echo "$ALL_PACKAGES" | jq -c '[.[] | select(.platforms | any(. == "darwin-x86_64" or . == "macos-x86_64"))]')

          FILTERED_COUNT=$(echo "$FILTERED_PACKAGES" | jq 'length')
          TOTAL_COUNT=$(echo "$ALL_PACKAGES" | jq 'length')

          if [ $FILTERED_COUNT -lt $TOTAL_COUNT ]; then
            SKIPPED=$(( TOTAL_COUNT - FILTERED_COUNT ))
            echo "⏭️  Skipping $SKIPPED packages that don't support $PLATFORM"
            echo "$ALL_PACKAGES" | jq -r '.[] | select(.platforms | any(. == "darwin-x86_64" or . == "macos-x86_64") | not) | "  - \(.package) (supports: \(.platforms | join(", ")))"'
          fi
          echo "filtered_packages=$FILTERED_PACKAGES" >> $GITHUB_OUTPUT
          echo "📦 Building $FILTERED_COUNT/$TOTAL_COUNT packages for $PLATFORM"

      - name: Build all packages in batch (macOS x86_64 only)
        if: fromJson(steps.filter-packages.outputs.filtered_packages)[0] != null
        run: |
          ./bin/potions build \
            --packages '${{ steps.filter-packages.outputs.filtered_packages }}' \
            --platform darwin-x86_64 \
            --recipes-dir recipes \
            --output-dir dist \
            --enable-security-scan \
            --timeout 20 \
            --successes build-successes.txt \
            --failures build-failures.txt \
            --timeouts build-failures-timeout.txt \
            --errors build-failures-error.txt

      - name: Create failure tracking artifact
        if: always()
        run: |
          # Extract failed package names from all failure files
          FAILED_PACKAGES="[]"

          if [ -f build-failures.txt ] && [ -s build-failures.txt ]; then
            FAILED=$(cat build-failures.txt | sed 's/^ *//' | jq -R . | jq -s -c .)
            FAILED_PACKAGES=$(echo "$FAILED_PACKAGES" | jq -c ". += $FAILED")
          fi

          if [ -f build-failures-timeout.txt ] && [ -s build-failures-timeout.txt ]; then
            FAILED=$(cat build-failures-timeout.txt | sed 's/^ *//' | jq -R . | jq -s -c .)
            FAILED_PACKAGES=$(echo "$FAILED_PACKAGES" | jq -c ". += $FAILED")
          fi

          if [ -f build-failures-error.txt ] && [ -s build-failures-error.txt ]; then
            FAILED=$(cat build-failures-error.txt | sed 's/^ *//' | jq -R . | jq -s -c .)
            FAILED_PACKAGES=$(echo "$FAILED_PACKAGES" | jq -c ". += $FAILED")
          fi

          # Deduplicate and sort
          FAILED_PACKAGES=$(echo "$FAILED_PACKAGES" | jq -c 'unique | sort')

          # Create tracking file
          cat > failure-tracking.json <<EOF
          {
            "platform": "macos-x86_64",
            "batch_id": "${{ matrix.batch.id }}",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "failed_packages": $FAILED_PACKAGES
          }
          EOF

          cat failure-tracking.json

      - name: Upload failure tracking
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: failure-tracking-macos-x86_64-batch-${{ matrix.batch.id }}
          path: failure-tracking.json
          retention-days: 3

      - name: Upload build failures report
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: macos-x86_64-batch-${{ matrix.batch.id }}-failures
          path: |
            build-failures.txt
            build-failures-timeout.txt
            build-failures-error.txt
          retention-days: 3
          if-no-files-found: ignore

      - name: Check for artifacts
        id: check-artifacts
        run: |
          if [ -n "$(find dist -name '*.tar.gz' -type f 2>/dev/null)" ]; then
            echo "has-artifacts=true" >> $GITHUB_OUTPUT

            # Calculate total artifact size
            TOTAL_SIZE=$(du -sh dist 2>/dev/null | cut -f1)
            ARTIFACT_COUNT=$(find dist -name '*.tar.gz' -type f 2>/dev/null | wc -l | tr -d ' ')
            echo "📊 Artifacts: $ARTIFACT_COUNT files, Total size: $TOTAL_SIZE"

            # Check for unusually large artifacts (>500MB warning)
            find dist -name '*.tar.gz' -type f -size +500M 2>/dev/null | while read large_file; do
              FILE_SIZE=$(du -h "$large_file" | cut -f1)
              echo "⚠️  Large artifact detected: $(basename "$large_file") ($FILE_SIZE)"
            done
          else
            echo "has-artifacts=false" >> $GITHUB_OUTPUT
            echo "ℹ️  No artifacts produced for this batch (likely custom build types)"
          fi

      - name: Upload artifacts
        if: success() && steps.check-artifacts.outputs.has-artifacts == 'true'
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: macos-x86_64-batch-${{ matrix.batch.id }}-builds
          path: dist/
          retention-days: 3
          include-hidden-files: false

      - name: Upload build completion marker
        if: success() && steps.check-artifacts.outputs.has-artifacts == 'true'
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: macos-x86_64-batch-${{ matrix.batch.id }}-complete
          path: build-successes.txt
          retention-days: 1

      - name: Cleanup build artifacts
        if: always()
        run: |
          echo "🧹 Cleaning up build directory..."
          if [ -d dist ]; then
            chmod -R u+w dist
            rm -rf dist
          fi
          echo "📊 Final disk space:"
          df -h
