name: yq
version:
  source: "github-release:mikefarah/yq"
  exclude_patterns: "(alpha|beta|rc|dev|preview|pre|snapshot|nightly|test|unstable|canary|next|edge|weekly|daily)"
  extract_pattern: '[0-9]+\.[0-9]+\.[0-9]+'

build_type: custom
description: "YAML processor (like jq for YAML)"

download:
  download_url: "https://github.com/mikefarah/yq/archive/refs/tags/v{version}.tar.gz"
  platforms:
    darwin-x86_64:
    darwin-arm64:
    linux-amd64:
    linux-arm64:

security:
  verify_signature: false
  # gpg_keys_url: ""  # Add URL to project KEYS file for auto-import
  scan_vulnerabilities: true

configure:
  script: |
    # yq is written in Go
    true

build:
  timeout_minutes: 10
  out_of_tree: false
  custom_build: |
    export GOCACHE=$(cd $PREFIX && pwd)/.cache/go-build
    export GOMODCACHE=$(cd $PREFIX && pwd)/pkg/mod
    mkdir -p $GOCACHE $GOMODCACHE
    go build -ldflags "-s -w" -o yq .
    mkdir -p $PREFIX/bin
    cp yq $PREFIX/bin/

dependencies:
  - go
